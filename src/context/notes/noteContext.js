import { createContext } from "react";

const noteContext = createContext();// syntax to create a new context, context is something that hold the state of that particular element

export default noteContext;